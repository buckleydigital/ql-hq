name = "qlhq-api-proxy"
main = "src/index.ts"
compatibility_date = "2025-04-01"

# Custom domain — configure in Cloudflare Dashboard → Workers → Triggers → Custom Domains
# after deploying. Point api.quoteleadshq.com to this worker.

[vars]
SUPABASE_FUNCTION_URL = "https://wjadekgptkstfdootuol.supabase.co/functions/v1/api"
